<?php

namespace FeedUtils;

/**
 * Provides access to items from feed, that are sorted from the newest (first)
 * to the oldest (last).
 */
interface IFeedProvider extends \IteratorAggregate {
    
}

